import { render } from "../../render";
import React from "react";

const App = () => {

    return (
        <div id='container'>
            <div>This is example.</div>
        </div>
    );
};

render(<App />);
